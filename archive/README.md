# Archive

Pages pulled off the live site (Sept 2026) but kept around in case they come back.

- `content/projects/`, `content/journal/` — the MDX files. Move back to `content/` to restore.
- `routes/projects/`, `routes/journal/` — the Next.js route files. Move back to `src/app/` to restore.
- `images/` — screenshots referenced by the project pages. Move back to `public/images/projects/` to restore.

Then re-add the nav links in `src/app/layout.tsx` and the routes in `src/app/sitemap.ts`.
